!!!
%html
  %head
    %title palinium.com
    = stylesheet_link_tag    "application", :media => "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body{:class => "blog #{controller_name} #{action_name}", :'data-view' => "#{controller_name.humanize}"}
    #container
      - unless Rails.env.production?
        #env_box= Rails.env
      %header
        = render :partial => "layouts/header"
      %section
        = yield
      %footer
        = render :partial => "layouts/footer"
